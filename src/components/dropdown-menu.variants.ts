// Dropdown and context menus share one surface; these aliases keep the older names public.
export {
  menuContentVariants as dropdownMenuContentVariants,
  menuItemVariants as dropdownMenuItemVariants,
} from './menu.variants';
